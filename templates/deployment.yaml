# StarSling - 3 Deployment: gateway, simulation, worldsvc

# --- Gateway Deployment ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-gateway
  labels:
    app: {{ .Release.Name }}
    component: gateway
spec:
  replicas: {{ .Values.gateway.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}
      component: gateway
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
        component: gateway
    spec:
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      {{- end }}
      containers:
        - name: gateway
          image: "{{ .Values.gateway.image.repository }}:{{ .Values.gateway.image.tag }}"
          imagePullPolicy: {{ .Values.gateway.image.pullPolicy }}
          ports:
            - containerPort: 8080
              protocol: TCP
          env:
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- range $key, $value := .Values.secretEnv }}
            - name: {{ $key }}
              valueFrom:
                secretKeyRef:
                  name: {{ $.Release.Name }}-secrets
                  key: {{ $key }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.gateway.resources | nindent 12 }}

---
# --- Simulation Deployment ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-simulation
  labels:
    app: {{ .Release.Name }}
    component: simulation
spec:
  replicas: {{ .Values.simulation.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}
      component: simulation
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
        component: simulation
    spec:
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      {{- end }}
      containers:
        - name: simulation
          image: "{{ .Values.simulation.image.repository }}:{{ .Values.simulation.image.tag }}"
          imagePullPolicy: {{ .Values.simulation.image.pullPolicy }}
          env:
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- range $key, $value := .Values.secretEnv }}
            - name: {{ $key }}
              valueFrom:
                secretKeyRef:
                  name: {{ $.Release.Name }}-secrets
                  key: {{ $key }}
            {{- end }}
          resources:
            {{- toYaml .Values.simulation.resources | nindent 12 }}

---
# --- WorldSvc Deployment ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-worldsvc
  labels:
    app: {{ .Release.Name }}
    component: worldsvc
spec:
  replicas: {{ .Values.worldsvc.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}
      component: worldsvc
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
        component: worldsvc
    spec:
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      {{- end }}
      containers:
        - name: worldsvc
          image: "{{ .Values.worldsvc.image.repository }}:{{ .Values.worldsvc.image.tag }}"
          imagePullPolicy: {{ .Values.worldsvc.image.pullPolicy }}
          ports:
            - containerPort: 8081
              protocol: TCP
          env:
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
            {{- range $key, $value := .Values.secretEnv }}
            - name: {{ $key }}
              valueFrom:
                secretKeyRef:
                  name: {{ $.Release.Name }}-secrets
                  key: {{ $key }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.worldsvc.resources | nindent 12 }}

---
# --- Frontend Deployment (Three.js game client) ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-frontend
  labels:
    app: {{ .Release.Name }}
    component: frontend
spec:
  replicas: {{ .Values.frontend.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}
      component: frontend
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
        component: frontend
    spec:
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      {{- end }}
      containers:
        - name: frontend
          image: "{{ .Values.frontend.image.repository }}:{{ .Values.frontend.image.tag }}"
          imagePullPolicy: {{ .Values.frontend.image.pullPolicy }}
          ports:
            - containerPort: 80
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 10
          resources:
            {{- toYaml .Values.frontend.resources | nindent 12 }}

---
# --- Landing Deployment ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-landing
  labels:
    app: {{ .Release.Name }}
    component: landing
spec:
  replicas: {{ .Values.landing.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}
      component: landing
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
        component: landing
    spec:
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      {{- end }}
      containers:
        - name: landing
          image: "{{ .Values.landing.image.repository }}:{{ .Values.landing.image.tag }}"
          imagePullPolicy: {{ .Values.landing.image.pullPolicy }}
          ports:
            - containerPort: 80
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /healthz
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 10
          resources:
            {{- toYaml .Values.landing.resources | nindent 12 }}
